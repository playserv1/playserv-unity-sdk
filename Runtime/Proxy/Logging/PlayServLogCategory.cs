namespace Playserv.Proxy.Logging
{
    /// <summary>
    /// Stable log categories used across PlayServ runtime subsystems.
    /// </summary>
    public enum PlayServLogCategory
    {
        General,
        Transport,
        Http,
        Rpc,
        Spawn,
        Events,
        Data
    }
}
